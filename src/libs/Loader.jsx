import '../styles/loader.css'; // Asegúrate de que el archivo CSS esté correctamente importado.

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
